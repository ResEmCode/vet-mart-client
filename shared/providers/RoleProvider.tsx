// import { useSession } from "next-auth/react";
// import { ReactNode } from "react";

// export const RoleProvider = ({children}: {children: ReactNode}) => {
//     const session = useSession();
    
//     return session.data?.user.role === "ADMIN" ? <Admin /> : 
// };